<div class="news-container">
    {{#each programas}}
        <div class="news-box">
            <img src="{{this.imagen}}" alt="Imagen de la noticia">
            <div class="news-box-text">
                <h3>{{this.titulo}}</h3>
                <p>{{this.descripcion}}</p>
                <a href="{{this.link}}">Ver más</a>
            </div>
        </div>
    {{/each}}
</div>

