{
  "builds":[
    {
      "src":"./server.js",
      "use":"@vercel/node",
      "config": {
        "buildCommand": "npm run install-and-start"
      }
    }
  ],
  "routes":[
    {
      "src":"/(.*)",
      "dest":"/"
    }
  ]
}
